<%= form_with(model: [survey,answer], local: true) do |form| %>
    <%= @textbox.subject %>
    <%= form.text_field :content %>
    <%= form.hidden_field :textbox_id, :value=>@textbox.id %>
      <%= form.submit %>
<% end %>
