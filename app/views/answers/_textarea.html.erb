<%= form_with(model: [survey,type,answer], local: true) do |form| %>
    <%= type.subject %>
    <%= form.text_area :content %>
    <%= form.hidden_field :textbox_id, :value=>type.id %>
      <%= form.submit %>
<% end %>
